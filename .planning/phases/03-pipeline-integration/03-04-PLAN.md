---
phase: 03-pipeline-integration
plan: 04
type: execute
wave: 3
depends_on: [03-02, 03-03]
files_modified:
  - documentation/source/conf.py
  - documentation/source/index.rst
  - documentation/source/cybir.rst
  - documentation/source/cybir.core.rst
  - documentation/source/cybir.core.ekc.rst
  - documentation/source/cybir.core.build_gv.rst
  - documentation/source/cybir.core.weyl.rst
  - documentation/source/cybir.core.patch.rst
  - documentation/source/cybir.core.types.rst
  - documentation/source/cybir.core.graph.rst
  - documentation/source/cybir.core.classify.rst
  - documentation/source/cybir.core.flop.rst
  - documentation/source/cybir.core.gv.rst
  - documentation/source/cybir.core.util.rst
  - documentation/Makefile
  - notebooks/h11_2_walkthrough.ipynb
  - notebooks/h11_3_walkthrough.ipynb
autonomous: true
requirements: [PKG-02, PKG-03]

must_haves:
  truths:
    - "Sphinx documentation builds cleanly with make html"
    - "API reference pages auto-generate from numpy-style docstrings"
    - "MathJax renders equation references from docstrings"
    - "h11=2 and h11=3 example notebooks exist and demonstrate the full pipeline"
  artifacts:
    - path: "documentation/source/conf.py"
      provides: "Sphinx configuration mirroring dbrane-tools"
      contains: "sphinx_book_theme"
    - path: "documentation/source/index.rst"
      provides: "Documentation index with toctree"
      contains: "toctree"
    - path: "notebooks/h11_2_walkthrough.ipynb"
      provides: "h11=2 example notebook"
    - path: "notebooks/h11_3_walkthrough.ipynb"
      provides: "h11=3 example notebook"
  key_links:
    - from: "documentation/source/conf.py"
      to: "cybir/"
      via: "autodoc imports cybir package"
      pattern: "autodoc"
---

<objective>
Create Sphinx documentation with API reference and two example notebooks (h11=2 and h11=3 walkthroughs).

Purpose: Documentation makes the package usable and discoverable. API reference auto-generates from the numpy-style docstrings already present. Notebooks demonstrate the complete pipeline.
Output: documentation/ directory with Sphinx config, RST files, Makefile. notebooks/ directory with two .ipynb files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-pipeline-integration/03-CONTEXT.md
@.planning/phases/03-pipeline-integration/03-RESEARCH.md

<interfaces>
<!-- Documentation pattern from dbrane-tools -->

From 03-RESEARCH.md (Documentation Pattern section, lines 579-628):
```python
# conf.py pattern
extensions = [
    "sphinx.ext.autodoc", "sphinx.ext.autosummary", "sphinx.ext.mathjax",
    "sphinx.ext.napoleon", "sphinx_copybutton", "sphinx_autodoc_typehints",
    "sphinx_togglebutton", "sphinx_design", "myst_nb",
]
html_theme = "sphinx_book_theme"
nb_execution_mode = "off"
```

Modules to document:
- cybir.core.ekc (CYBirationalClass)
- cybir.core.build_gv (setup_root, construct_phases)
- cybir.core.weyl (expand_weyl)
- cybir.core.patch (patch_cytools)
- cybir.core.types (CalabiYauLite, ExtremalContraction, ContractionType)
- cybir.core.graph (CYGraph)
- cybir.core.classify (classify_contraction, etc.)
- cybir.core.flop (wall_cross_intnums, wall_cross_c2, flop_phase)
- cybir.core.gv (gv_series, gv_eff, is_potent, is_nilpotent)
- cybir.core.util (normalize_curve, coxeter_reflection, etc.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Sphinx documentation structure</name>
  <files>documentation/source/conf.py, documentation/source/index.rst, documentation/source/cybir.rst, documentation/source/cybir.core.rst, documentation/source/cybir.core.ekc.rst, documentation/source/cybir.core.build_gv.rst, documentation/source/cybir.core.weyl.rst, documentation/source/cybir.core.patch.rst, documentation/source/cybir.core.types.rst, documentation/source/cybir.core.graph.rst, documentation/source/cybir.core.classify.rst, documentation/source/cybir.core.flop.rst, documentation/source/cybir.core.gv.rst, documentation/source/cybir.core.util.rst, documentation/Makefile</files>
  <read_first>
    - /Users/elijahsheridan/Research/string/cytools_code/cornell-dev/projects/vex/elijah/dbrane-tools/documentation/source/conf.py
    - .planning/phases/03-pipeline-integration/03-RESEARCH.md (Documentation Pattern section, lines 579-628)
  </read_first>
  <action>
**Create `documentation/source/conf.py` per D-18, D-20:**

```python
import os
import sys
sys.path.insert(0, os.path.abspath('../../'))

project = "cybir"
copyright = "2026, Elijah Sheridan"
author = "Elijah Sheridan"

extensions = [
    "sphinx.ext.autodoc",
    "sphinx.ext.autosummary",
    "sphinx.ext.mathjax",
    "sphinx.ext.napoleon",
    "sphinx_copybutton",
    "sphinx_autodoc_typehints",
    "sphinx_togglebutton",
    "sphinx_design",
    "myst_nb",
]

source_suffix = [".rst", ".ipynb", ".md"]
autodoc_default_flags = ["members"]
autosummary_generate = True
napoleon_use_rtype = False
napoleon_custom_sections = [('Returns', 'params_style')]

html_theme = "sphinx_book_theme"
nb_execution_mode = "off"
myst_enable_extensions = ["dollarmath"]
myst_dmath_double_inline = True
add_module_names = False
toc_object_entries_show_parents = "hide"

exclude_patterns = ["_build"]
```

**Create `documentation/Makefile`:**
```makefile
SPHINXOPTS    ?=
SPHINXBUILD   ?= sphinx-build
SOURCEDIR     = source
BUILDDIR      = build

help:
	@$(SPHINXBUILD) -M help "$(SOURCEDIR)" "$(BUILDDIR)" $(SPHINXOPTS)

.PHONY: help Makefile

%: Makefile
	@$(SPHINXBUILD) -M $@ "$(SOURCEDIR)" "$(BUILDDIR)" $(SPHINXOPTS)
```

**Create `documentation/source/index.rst`:**
```rst
cybir
=====

**cybir** -- GV-based extended Kahler cone construction for Calabi-Yau threefolds.

A Python package for studying the birational geometry of CY3 hypersurfaces
in toric varieties, following arXiv:2212.10573 and arXiv:2303.00757.
Built to integrate cleanly with CYTools.

.. toctree::
   :maxdepth: 2
   :caption: API Reference

   cybir
   cybir.core

.. toctree::
   :maxdepth: 1
   :caption: Examples

   ../../notebooks/h11_2_walkthrough
   ../../notebooks/h11_3_walkthrough
```

**Create one RST file per module**, each following the pattern:
```rst
cybir.core.ekc
==============

.. automodule:: cybir.core.ekc
   :members:
   :undoc-members:
   :show-inheritance:
```

Create these RST files:
- `cybir.rst` -- top-level package page with `automodule:: cybir`
- `cybir.core.rst` -- subpackage page with toctree to all module pages
- `cybir.core.ekc.rst`
- `cybir.core.build_gv.rst`
- `cybir.core.weyl.rst`
- `cybir.core.patch.rst`
- `cybir.core.types.rst`
- `cybir.core.graph.rst`
- `cybir.core.classify.rst`
- `cybir.core.flop.rst`
- `cybir.core.gv.rst`
- `cybir.core.util.rst`

The `cybir.core.rst` file should have:
```rst
cybir.core
==========

.. toctree::
   :maxdepth: 1

   cybir.core.ekc
   cybir.core.build_gv
   cybir.core.weyl
   cybir.core.patch
   cybir.core.types
   cybir.core.graph
   cybir.core.classify
   cybir.core.flop
   cybir.core.gv
   cybir.core.util
```

Verify doc build: `cd documentation && make html` should complete without errors (warnings about missing CYTools types are OK).
  </action>
  <verify>
    <automated>cd /Users/elijahsheridan/Research/string/cytools_code/ekc/documentation && conda run -n cytools make html 2>&1 | tail -5</automated>
  </verify>
  <done>
- `documentation/source/conf.py` exists with sphinx-book-theme and all extensions
- `documentation/source/index.rst` exists with toctree
- One RST file per cybir module
- `make html` completes (warnings OK, no errors)
  </done>
  <acceptance_criteria>
- `grep "sphinx_book_theme" documentation/source/conf.py` returns a match
- `grep "myst_nb" documentation/source/conf.py` returns a match
- `grep "nb_execution_mode.*off" documentation/source/conf.py` returns a match
- `ls documentation/source/cybir.core.ekc.rst` exits 0
- `ls documentation/source/cybir.core.types.rst` exits 0
- `ls documentation/Makefile` exits 0
- `grep "toctree" documentation/source/index.rst` returns a match
- `cd documentation && make html` exits 0 (ignoring warnings)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create example notebooks</name>
  <files>notebooks/h11_2_walkthrough.ipynb, notebooks/h11_3_walkthrough.ipynb</files>
  <read_first>
    - .planning/phases/03-pipeline-integration/03-RESEARCH.md (Notebook Structure section, lines 630-643)
    - .planning/phases/03-pipeline-integration/03-CONTEXT.md (D-19)
    - cybir/core/ekc.py
  </read_first>
  <action>
**Create `notebooks/h11_2_walkthrough.ipynb` and `notebooks/h11_3_walkthrough.ipynb` per D-19.**

These are pre-executed notebooks (will be run manually by the user, not during doc build). Create them as valid .ipynb JSON with markdown and code cells but **empty outputs** (the user will execute them when CYTools is available with actual polytope data).

**h11_2_walkthrough.ipynb structure:**

Cells (markdown + code alternating):

1. **Markdown**: Title "# EKC Construction: h11=2 Example". Brief intro referencing arXiv:2212.10573.
2. **Code**: Imports:
   ```python
   import cybir
   from cybir import CYBirationalClass, patch_cytools
   import cytools
   import numpy as np
   
   # Activate CYTools integration
   patch_cytools()
   ```
3. **Markdown**: "## Load a Calabi-Yau threefold" -- explain loading an h11=2 polytope
4. **Code**: Load polytope and CY:
   ```python
   # Load an h11=2 polytope from CYTools database
   p = cytools.fetch_polytopes(h11=2, limit=1)[0]
   cy = p.triangulate().get_cy()
   print(f"h11 = {cy.h11()}, h21 = {cy.h21()}")
   ```
5. **Markdown**: "## Construct the Extended Kahler Cone"
6. **Code**: 
   ```python
   ekc = CYBirationalClass.from_gv(cy, max_deg=10, verbose=True)
   print(ekc)
   ```
7. **Markdown**: "## Inspect Phases"
8. **Code**:
   ```python
   for phase in ekc.phases:
       print(f"{phase.label}: int_nums shape = {phase.int_nums.shape}")
   print(f"\nTotal phases: {len(ekc.phases)}")
   ```
9. **Markdown**: "## Inspect Contractions"
10. **Code**:
    ```python
    for c in ekc.contractions:
        print(f"  {c.contraction_type.display_name()}: curve = {c.flopping_curve}")
    ```
11. **Markdown**: "## Phase Graph Structure"
12. **Code**:
    ```python
    print(ekc.graph)
    for phase in ekc.phases:
        neighbors = ekc.graph.neighbors(phase.label)
        print(f"{phase.label} -> {[n.label for n in neighbors]}")
    ```
13. **Markdown**: "## Coxeter Data"
14. **Code**:
    ```python
    if ekc.coxeter_matrix is not None:
        print("Coxeter matrix:")
        print(ekc.coxeter_matrix)
    else:
        print("No Coxeter reflections found (all walls are terminal or generic flops)")
    ```
15. **Markdown**: "## Weyl Expansion (optional)"
16. **Code**:
    ```python
    ekc.expand_weyl()
    print(f"After Weyl expansion: {len(ekc.phases)} phases")
    ```

**h11_3_walkthrough.ipynb structure:**

Same overall flow but:
- Title: "# EKC Construction: h11=3 Example"
- Load h11=3 polytope: `p = cytools.fetch_polytopes(h11=3, limit=1)[0]`
- Additional cells showing:
  - Multiple flop phases (expected with h11=3)
  - The full BFS build log: `for entry in ekc.build_log: print(entry)`
  - Contraction type distribution: count of each type
  - The Weyl expansion effect (likely more dramatic with h11=3)

**Notebook JSON format:**
Use valid nbformat 4.5 JSON. Each notebook must have:
```json
{
  "nbformat": 4,
  "nbformat_minor": 5,
  "metadata": {
    "kernelspec": {
      "display_name": "Python 3 (cytools)",
      "language": "python",
      "name": "python3"
    },
    "language_info": {
      "name": "python",
      "version": "3.12.12"
    }
  },
  "cells": [...]
}
```

Each cell is either:
```json
{"cell_type": "markdown", "metadata": {}, "source": ["# Title\n", "Text..."]}
```
or:
```json
{"cell_type": "code", "metadata": {}, "source": ["code line 1\n", "code line 2"], "outputs": [], "execution_count": null}
```
  </action>
  <verify>
    <automated>cd /Users/elijahsheridan/Research/string/cytools_code/ekc && conda run -n cytools python -c "import json; nb=json.load(open('notebooks/h11_2_walkthrough.ipynb')); assert nb['nbformat']==4; print(f'h11_2: {len(nb[\"cells\"])} cells')" && conda run -n cytools python -c "import json; nb=json.load(open('notebooks/h11_3_walkthrough.ipynb')); assert nb['nbformat']==4; print(f'h11_3: {len(nb[\"cells\"])} cells')"</automated>
  </verify>
  <done>
- `notebooks/h11_2_walkthrough.ipynb` exists as valid nbformat 4 JSON
- `notebooks/h11_3_walkthrough.ipynb` exists as valid nbformat 4 JSON
- h11=2 notebook demonstrates: import, load CY, construct EKC, inspect phases/contractions/graph/Coxeter, Weyl expansion
- h11=3 notebook demonstrates the same plus build log, type distribution, more complex graph
- Outputs are empty (user will execute manually)
  </done>
  <acceptance_criteria>
- `python -c "import json; json.load(open('notebooks/h11_2_walkthrough.ipynb'))"` exits 0
- `python -c "import json; json.load(open('notebooks/h11_3_walkthrough.ipynb'))"` exits 0
- `grep -c "CYBirationalClass" notebooks/h11_2_walkthrough.ipynb` returns at least 1
- `grep -c "patch_cytools" notebooks/h11_2_walkthrough.ipynb` returns at least 1
- `grep -c "expand_weyl" notebooks/h11_2_walkthrough.ipynb` returns at least 1
- `grep -c "h11=3" notebooks/h11_3_walkthrough.ipynb` returns at least 1
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Documentation and notebooks are non-executable artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | I (Info Disclosure) | Notebooks | accept | Notebooks contain example code, no secrets. Pre-executed outputs may contain polytope data which is public. |
</threat_model>

<verification>
- `cd documentation && make html` completes
- Both notebooks are valid JSON
- Notebooks contain the expected API calls
</verification>

<success_criteria>
Sphinx documentation builds cleanly with API reference for all modules. Two example notebooks exist demonstrating the full EKC pipeline for h11=2 and h11=3.
</success_criteria>

<output>
After completion, create `.planning/phases/03-pipeline-integration/03-04-SUMMARY.md`
</output>
