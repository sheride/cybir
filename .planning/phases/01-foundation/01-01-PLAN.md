---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - cybir/__init__.py
  - cybir/core/__init__.py
  - cybir/core/types.py
  - cybir/core/flop.py
  - cybir/core/ekc.py
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_types.py
autonomous: true
requirements:
  - PKG-01
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
  - DATA-06

must_haves:
  truths:
    - "pip install -e . succeeds in cytools conda env and import cybir works"
    - "CalabiYauLite can be instantiated with int_nums and optional fields, and properties return correct values"
    - "CalabiYauLite.freeze() prevents further attribute modification"
    - "ExtremalContraction is frozen by default after construction"
    - "ContractionType has exactly 5 members and display_name works for both notations"
    - "InsufficientGVError is a RuntimeError subclass"
    - "CalabiYauLite __eq__ uses np.allclose on int_nums, __hash__ uses label"
  artifacts:
    - path: "pyproject.toml"
      provides: "Package metadata and build config"
      contains: "hatchling"
    - path: "cybir/__init__.py"
      provides: "Package entry point with version"
      contains: "__version__"
    - path: "cybir/core/types.py"
      provides: "All Phase 1 data types"
      exports: ["CalabiYauLite", "ExtremalContraction", "ContractionType", "InsufficientGVError"]
    - path: "tests/test_types.py"
      provides: "Type tests"
      min_lines: 80
  key_links:
    - from: "cybir/__init__.py"
      to: "cybir/core/types.py"
      via: "re-export"
      pattern: "from cybir\\.core\\.types import"
    - from: "cybir/core/__init__.py"
      to: "cybir/core/types.py"
      via: "re-export"
      pattern: "from \\.types import"
---

<objective>
Create the cybir package skeleton and all core data types (CalabiYauLite, ExtremalContraction, ContractionType, InsufficientGVError) with freeze/immutability support, plus a comprehensive test suite for those types.

Purpose: Establishes the installable package and the data containers that all subsequent phases build upon. Without correct, tested types, Phases 2 and 3 have no foundation.
Output: An installable package with 4 data types, freeze mechanism, and passing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- dbrane-tools CalabiYauLite interface to match (cybir adds gv_invariants and label fields) -->
From /Users/elijahsheridan/Research/string/cytools_code/dbrane-tools/dbrane_tools/core/geometry.py:
```python
class CalabiYauLite():
    def __init__(
            self, int_nums, c2=None, kahler_cone=None,
            mori_cone=None, polytope=None, charges=None,
            indices=None, eff_cone=None,
            triangulation=None, fan=None):
        self._int_nums = int_nums
        self._c2 = c2
        # ... private attrs for all fields ...

    @property
    def int_nums(self):
        return self._int_nums

    @property
    def c2(self):
        return self._c2

    # properties for: kahler_cone, mori_cone, charges, indices, eff_cone, polytope, triangulation, fan
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create package skeleton and pyproject.toml</name>
  <files>pyproject.toml, cybir/__init__.py, cybir/core/__init__.py, cybir/core/flop.py, cybir/core/ekc.py, tests/__init__.py, tests/conftest.py</files>
  <read_first>
    /Users/elijahsheridan/Research/string/cytools_code/dbrane-tools/dbrane_tools/core/geometry.py
    /Users/elijahsheridan/Research/string/cytools_code/ekc/.planning/phases/01-foundation/01-RESEARCH.md
  </read_first>
  <action>
Create the directory structure and files per D-01 and D-02:

1. **pyproject.toml** (per PKG-01): Use hatchling build backend. Metadata:
   - name: "cybir"
   - version: "0.1.0"
   - description: "GV-based extended Kahler cone construction for Calabi-Yau threefolds"
   - requires-python: ">=3.12"
   - dependencies: ["numpy>=1.24,<3", "scipy>=1.10", "python-flint>=0.7,<0.9", "hsnf>=0.3", "sympy>=1.12", "networkx>=3.0"]
   - [project.optional-dependencies] dev = ["pytest>=8.0", "pytest-cov>=5.0", "ruff>=0.4"]
   - [build-system] requires = ["hatchling>=1.21"], build-backend = "hatchling.build"
   - Do NOT list cytools as a dependency (it is a local prerequisite, per research)

2. **cybir/__init__.py**: Set `__version__ = "0.1.0"`. Import and re-export from cybir.core: CalabiYauLite, ExtremalContraction, ContractionType, InsufficientGVError.

3. **cybir/core/__init__.py**: Import and re-export CalabiYauLite, ExtremalContraction, ContractionType, InsufficientGVError from .types.

4. **cybir/core/flop.py**: Empty placeholder with docstring: """Wall-crossing, contraction diagnosis, and GV math (Phase 2)."""

5. **cybir/core/ekc.py**: Empty placeholder with docstring: """ExtendedKahlerCone orchestrator (Phase 3)."""

6. **tests/__init__.py**: Empty file.

7. **tests/conftest.py**: Create shared pytest fixtures:
   - `sample_int_nums`: returns `np.array([[[0, 1], [1, 0]], [[1, 0], [0, 2]]])` (a simple 2x2x2 symmetric tensor for h11=2)
   - `sample_c2`: returns `np.array([24, 44])` (typical c2 values for h11=2)
   - `sample_cyl`: returns a CalabiYauLite constructed with sample_int_nums and sample_c2 and label="phase_0"
  </action>
  <verify>
    <automated>cd /Users/elijahsheridan/Research/string/cytools_code/ekc && conda run -n cytools pip install -e . 2>&1 | tail -3 && conda run -n cytools python -c "import cybir; print(cybir.__version__)"</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml exists and contains `build-backend = "hatchling.build"`
    - pyproject.toml contains `name = "cybir"` and `version = "0.1.0"`
    - pyproject.toml dependencies list includes "numpy>=1.24,<3", "hsnf>=0.3", "networkx>=3.0"
    - pyproject.toml does NOT contain "cytools" in dependencies
    - cybir/__init__.py contains `__version__ = "0.1.0"`
    - cybir/__init__.py contains `from cybir.core.types import CalabiYauLite`
    - cybir/core/__init__.py contains `from .types import CalabiYauLite`
    - cybir/core/flop.py exists with a docstring mentioning "Phase 2"
    - cybir/core/ekc.py exists with a docstring mentioning "Phase 3"
    - tests/conftest.py contains `def sample_int_nums` and `def sample_cyl`
    - `pip install -e .` succeeds and `import cybir` prints "0.1.0"
  </acceptance_criteria>
  <done>Package installs in cytools env, import cybir works, version is 0.1.0, all skeleton files exist</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement core data types in types.py</name>
  <files>cybir/core/types.py, tests/test_types.py</files>
  <read_first>
    /Users/elijahsheridan/Research/string/cytools_code/dbrane-tools/dbrane_tools/core/geometry.py
    /Users/elijahsheridan/Research/string/cytools_code/ekc/.planning/phases/01-foundation/01-CONTEXT.md
    /Users/elijahsheridan/Research/string/cytools_code/ekc/.planning/phases/01-foundation/01-RESEARCH.md
  </read_first>
  <behavior>
    CalabiYauLite:
    - Test: CalabiYauLite(int_nums=np.zeros((2,2,2))) creates object, .int_nums returns copy of input
    - Test: CalabiYauLite with all optional fields (c2, kahler_cone, mori_cone, polytope, charges, indices, eff_cone, triangulation, fan, gv_invariants, label) -- each accessible via property
    - Test: .int_nums returns np.copy (modifying returned array does not change internal state)
    - Test: .freeze() then setting any attribute raises AttributeError with "frozen" in message
    - Test: Before freeze, setting _int_nums directly works (mutable during construction)
    - Test: __eq__ with same int_nums returns True, different int_nums returns False
    - Test: __hash__ returns hash of label, two objects with same label have same hash
    - Test: __repr__ includes class name and label

    ExtremalContraction:
    - Test: ExtremalContraction(flopping_curve=np.array([1,0])) creates frozen object
    - Test: All optional fields (start_phase, end_phase, contraction_type, gv_invariant, effective_gv, zero_vol_divisor, coxeter_reflection) default to None
    - Test: Setting any attribute after construction raises AttributeError with "frozen" in message
    - Test: __repr__ includes flopping_curve and contraction_type

    ContractionType:
    - Test: len(ContractionType) == 5
    - Test: ContractionType.ASYMPTOTIC.value == "asymptotic"
    - Test: ContractionType.CFT.value == "CFT"
    - Test: ContractionType.SU2.value == "su2"
    - Test: ContractionType.SYMMETRIC_FLOP.value == "symmetric_flop"
    - Test: ContractionType.FLOP.value == "flop"
    - Test: ContractionType.FLOP.display_name("wilson") == "Flop"
    - Test: ContractionType.FLOP.display_name("paper") == "generic flop"
    - Test: ContractionType.ASYMPTOTIC.display_name("wilson") == "Type III"
    - Test: ContractionType.CFT.display_name("wilson") == "Type II"
    - Test: ContractionType.SU2.display_name("wilson") == "Type I"
    - Test: ContractionType.SU2.display_name("paper") == "su(2) enhancement"
    - Test: ContractionType.SYMMETRIC_FLOP.display_name("paper") == "symmetric flop"
    - Test: default notation (no arg) returns paper notation

    InsufficientGVError:
    - Test: InsufficientGVError is subclass of RuntimeError
    - Test: InsufficientGVError("msg") stores message correctly
    - Test: isinstance(InsufficientGVError("x"), RuntimeError) is True
  </behavior>
  <action>
Implement `cybir/core/types.py` with all 4 types per D-04 through D-07:

**InsufficientGVError** (per D-07):
```python
class InsufficientGVError(RuntimeError):
    """Raised when GV series has not been computed to high enough degree."""
    pass
```

**ContractionType** (per D-06): Use `enum.Enum` with 5 members. For the notation dicts, use module-level constants to avoid the enum-member-shadowing pitfall (Pitfall 4 from research):
```python
_WILSON_NOTATION = {
    "ASYMPTOTIC": "Type III",
    "CFT": "Type II",
    "SU2": "Type I",
    "SYMMETRIC_FLOP": "Symmetric Flop",
    "FLOP": "Flop",
}
_PAPER_NOTATION = {
    "ASYMPTOTIC": "asymptotic",
    "CFT": "CFT",
    "SU2": "su(2) enhancement",
    "SYMMETRIC_FLOP": "symmetric flop",
    "FLOP": "generic flop",
}

class ContractionType(enum.Enum):
    ASYMPTOTIC = "asymptotic"
    CFT = "CFT"
    SU2 = "su2"
    SYMMETRIC_FLOP = "symmetric_flop"
    FLOP = "flop"

    def display_name(self, notation="paper"):
        if notation == "wilson":
            return _WILSON_NOTATION[self.name]
        return _PAPER_NOTATION[self.name]
```

**CalabiYauLite** (per D-04): Follow dbrane-tools private-attribute + property pattern. Constructor takes: int_nums (required), c2, kahler_cone, mori_cone, polytope, charges, indices, eff_cone, triangulation, fan, gv_invariants, label (all optional, default None). Set `self._frozen = False` at end of __init__.

Properties: All 12 fields. For int_nums and c2, return `np.copy(self._X)` if `self._X is not None` else None (defense-in-depth per Pitfall 3 in research). For Cone objects (kahler_cone, mori_cone, eff_cone) and other non-array fields, return directly.

`freeze()` method: sets `self._frozen = True`.

`__setattr__` override (per DATA-06): If `_frozen` is True and attribute name is not `_frozen`, raise `AttributeError(f"Cannot modify frozen CalabiYauLite (attribute '{name}')")`.

`__eq__`: Compare int_nums via `np.allclose(self.int_nums, other.int_nums)` and c2 via `np.allclose(self.c2, other.c2)` if both have c2, else just int_nums (per Pitfall 5 from research). Return NotImplemented if other is not CalabiYauLite.

`__hash__`: Return `hash(self._label)`.

`__repr__`: Return `f"CalabiYauLite(label={self._label!r})"`.

**ExtremalContraction** (per D-05): Constructor takes flopping_curve (required), start_phase, end_phase, contraction_type, gv_invariant, effective_gv, zero_vol_divisor, coxeter_reflection (all optional, default None). Set `self._frozen = True` at END of __init__ (frozen by default).

Properties for all 8 fields. Same `__setattr__` override pattern as CalabiYauLite.

`__repr__`: Return `f"ExtremalContraction(flopping_curve={self._flopping_curve}, type={self._contraction_type})"`.

Then write `tests/test_types.py` implementing all the behavior tests listed above. Use the conftest fixtures where appropriate. Import types from `cybir.core.types`.
  </action>
  <verify>
    <automated>cd /Users/elijahsheridan/Research/string/cytools_code/ekc && conda run -n cytools python -m pytest tests/test_types.py -v --tb=short 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - cybir/core/types.py contains `class CalabiYauLite` with `def freeze(self)`
    - cybir/core/types.py contains `class ExtremalContraction` with `self._frozen = True` in __init__
    - cybir/core/types.py contains `class ContractionType(enum.Enum)` with exactly 5 members
    - cybir/core/types.py contains `class InsufficientGVError(RuntimeError)`
    - cybir/core/types.py contains `_WILSON_NOTATION` and `_PAPER_NOTATION` as module-level dicts
    - cybir/core/types.py CalabiYauLite.__init__ accepts gv_invariants and label parameters
    - cybir/core/types.py CalabiYauLite.__eq__ contains `np.allclose`
    - cybir/core/types.py CalabiYauLite.__hash__ contains `hash(self._label)`
    - cybir/core/types.py CalabiYauLite.int_nums property contains `np.copy`
    - tests/test_types.py contains at least 15 test functions
    - All tests in tests/test_types.py pass
  </acceptance_criteria>
  <done>All 4 data types implemented with freeze mechanism, all tests pass, types importable from cybir and cybir.core</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input to CalabiYauLite | Numpy arrays from CYTools or user code |
| Freeze enforcement | Mutable -> immutable state transition |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | CalabiYauLite frozen state | mitigate | __setattr__ override prevents attribute mutation after freeze(); test verifies AttributeError raised |
| T-01-02 | Tampering | Returned numpy arrays | mitigate | int_nums and c2 properties return np.copy(), test verifies mutation of returned array does not affect internal state |
| T-01-03 | Tampering | ExtremalContraction fields | mitigate | Frozen by default in __init__, __setattr__ override prevents all post-construction mutation |
</threat_model>

<verification>
1. `pip install -e .` succeeds in cytools conda env
2. `python -c "import cybir; print(cybir.__version__)"` prints "0.1.0"
3. `python -c "from cybir import CalabiYauLite, ExtremalContraction, ContractionType, InsufficientGVError"` succeeds
4. `pytest tests/test_types.py -v` -- all tests pass
</verification>

<success_criteria>
- Package installs and imports work
- All 4 data types exist with documented interfaces
- Freeze mechanism prevents mutation after freeze
- ContractionType has 5 members with dual notation
- 15+ tests pass covering instantiation, immutability, equality, and enum behavior
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
