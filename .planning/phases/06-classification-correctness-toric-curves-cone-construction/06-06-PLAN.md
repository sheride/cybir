---
phase: 06-classification-correctness-toric-curves-cone-construction
plan: 06
type: execute
wave: 5
depends_on: ["06-05"]
files_modified:
  - tests/survey_h11_3.py
  - tests/compare_orbit.py
  - tests/run_compare_orbit.py
autonomous: false
requirements: []

must_haves:
  truths:
    - "survey_h11_3.py detects and counts GROSS_FLOP contractions"
    - "compare_orbit.py passes on all 243 polytopes (including the 7 previously mismatched)"
    - "Survey JSONL output includes gross_flop counts"
  artifacts:
    - path: "tests/survey_h11_3.py"
      provides: "Updated survey with GrossFlop detection"
      contains: "GROSS_FLOP"
    - path: "tests/compare_orbit.py"
      provides: "Updated orbit comparison"
  key_links:
    - from: "tests/survey_h11_3.py"
      to: "cybir.core.types.ContractionType.GROSS_FLOP"
      via: "contraction type counting"
      pattern: "GROSS_FLOP"
---

<objective>
Re-validate h11=3 survey after GrossFlop fix: update survey script to detect GROSS_FLOP, re-run compare_orbit.py to confirm all 7 mismatches resolved.

Purpose: Verify that the GrossFlop classification fix resolves all known misclassifications, and update the survey infrastructure to track GROSS_FLOP statistics.
Output: Updated survey and comparison scripts, re-validation results.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-classification-correctness-toric-curves-cone-construction/06-CONTEXT.md
@.planning/phases/06-classification-correctness-toric-curves-cone-construction/06-RESEARCH.md

<interfaces>
From tests/survey_h11_3.py:
```python
# Collects per-polytope: phase_count, contraction type counts, timing, errors
# Writes JSONL output
```

From tests/compare_orbit.py:
```python
# Compares cybir apply_coxeter_orbit against original ignore_sym=False
# Reports match/mismatch per polytope
```

From cybir/core/types.py:
```python
class ContractionType(enum.Enum):
    # ... including GROSS_FLOP = "gross_flop" (added in Plan 01)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update survey and comparison scripts for GROSS_FLOP</name>
  <files>tests/survey_h11_3.py, tests/compare_orbit.py</files>
  <read_first>tests/survey_h11_3.py, tests/compare_orbit.py, cybir/core/types.py</read_first>
  <action>
1. **Update tests/survey_h11_3.py:**
   - Find the section that counts contraction types (likely iterates over `ekc.contractions` and counts by `contraction_type`)
   - Add `GROSS_FLOP` to the counted types
   - In the JSONL output dict, add `"gross_flop_count"` field
   - Log GROSS_FLOP count in the per-polytope summary
   - Example: where the script currently has lines like:
     ```python
     sym_flop_count = sum(1 for c in ekc.contractions if c.contraction_type == ContractionType.SYMMETRIC_FLOP)
     ```
     Add:
     ```python
     gross_flop_count = sum(1 for c in ekc.contractions if c.contraction_type == ContractionType.GROSS_FLOP)
     ```
     And include `"gross_flop_count": gross_flop_count` in the output dict.

2. **Update tests/compare_orbit.py:**
   - The comparison script compares phase counts between cybir and original code
   - After the GrossFlop fix, the 7 previously mismatched polytopes (#22, #38, #39, #88, #89, #156, and one more) should now match
   - Ensure the script handles the GROSS_FLOP contraction type in its reporting
   - No logic changes needed -- the fix in classify.py/build_gv.py should make the comparison pass automatically
   - If the script filters contraction types, add GROSS_FLOP to the list

3. **Update tests/run_compare_orbit.py** if it has hardcoded type lists:
   - Add GROSS_FLOP to any type enumerations
   - Verify the timeout is sufficient (120s per polytope per D-13)
  </action>
  <verify>
    <automated>conda run -n cytools python -c "
from cybir.core.types import ContractionType
assert hasattr(ContractionType, 'GROSS_FLOP')
print('GROSS_FLOP enum exists')
import ast
with open('tests/survey_h11_3.py') as f:
    ast.parse(f.read())
print('survey_h11_3.py parses OK')
with open('tests/compare_orbit.py') as f:
    ast.parse(f.read())
print('compare_orbit.py parses OK')
" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - survey_h11_3.py contains `GROSS_FLOP` or `gross_flop` in contraction type counting
    - survey_h11_3.py output dict contains `gross_flop` key
    - compare_orbit.py parses without syntax errors
    - Both scripts are importable/parseable
  </acceptance_criteria>
  <done>Survey and comparison scripts updated to handle GROSS_FLOP contraction type</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run h11=3 re-validation</name>
  <files>tests/survey_h11_3.py, tests/compare_orbit.py</files>
  <action>
Run the h11=3 re-validation suite to verify the GrossFlop fix resolves all 7 known misclassifications:

1. Run compare_orbit.py via run_compare_orbit.py with 120s timeout per polytope
2. Run survey_h11_3.py on the 7 known problem polytopes (22, 38, 39, 88, 89, 156, and the 7th)
3. Compare new results against Phase 5 results to ensure no regressions
  </action>
  <what-built>
GrossFlop classification fix (condition b Kahler cone check), GROSS_FLOP enum, updated survey and comparison scripts. The fix should resolve all 7 previously mismatched h11=3 polytopes (#22, #38, #39, #88, #89, #156, and one more).
  </what-built>
  <how-to-verify>
1. Run compare_orbit.py on all polytopes with symmetric flops:
   ```bash
   conda run -n cytools python tests/run_compare_orbit.py --timeout 120 2>&1 | tee /tmp/compare_orbit_06.log
   ```
   Expected: ALL polytopes show "MATCH" (including the 7 that previously showed "MISMATCH").

2. Run survey_h11_3.py on a subset (e.g., the 7 known problem polytopes):
   ```bash
   conda run -n cytools python tests/survey_h11_3.py --polytopes 22,38,39,88,89,156 2>&1 | tee /tmp/survey_grossflop.log
   ```
   Expected: These polytopes now show `gross_flop_count > 0` where they previously showed `sym_flop_count > 0`.

3. Check that no other polytopes changed classification:
   Compare the new survey results against the Phase 5 results (`tests/survey_h11_3_results.jsonl`).
   Only the 7 problem polytopes should differ.
  </how-to-verify>
  <verify>
    <automated>echo "Checkpoint: requires human verification of re-validation results"</automated>
  </verify>
  <acceptance_criteria>
    - compare_orbit.py shows 0 mismatches across all polytopes with symmetric flops
    - The 7 previously mismatched polytopes now match
    - No regressions in other polytopes
  </acceptance_criteria>
  <done>All 7 previously mismatched h11=3 polytopes now match after GrossFlop fix, no regressions</done>
  <resume-signal>Type "approved" if all 7 mismatches resolved, or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Original code comparison | Comparing against potentially buggy original code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-13 | T (Tampering) | survey results | accept | JSONL output is append-only, previous results preserved for comparison |
</threat_model>

<verification>
- compare_orbit.py shows 0 mismatches on all 243 polytopes
- survey_h11_3.py output includes gross_flop counts
- Only the 7 known problem polytopes changed classification
</verification>

<success_criteria>
- All 7 previously mismatched polytopes now match in compare_orbit.py
- No regressions: all other polytopes still match
- GROSS_FLOP contraction type detected and counted in survey output
- User verifies results match expectations
</success_criteria>

<output>
After completion, create `.planning/phases/06-classification-correctness-toric-curves-cone-construction/06-06-SUMMARY.md`
</output>
